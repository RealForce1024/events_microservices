spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gomaster-me/events_microservices/config-repo
server:
  port: 8888
---
spring:
  profiles: docker
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gomaster-me/events_microservices/config-repo

